// Tiny RPC schema — same surface as tiny_rest, but with the
// `transport rpc` directive so the generator emits the RPC client.

transport rpc

model Widget {
  id Int @id
  name String
  weight Int?

  @@allow("create", auth() != null)
}

procedure echoName(name: String): String
