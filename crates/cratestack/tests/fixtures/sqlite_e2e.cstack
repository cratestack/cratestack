datasource db {
  provider = "sqlite"
  url = env("DATABASE_URL")
}

auth SessionUser {
  id Int
  role String
}

model Account {
  id Int @id
  ownerName String
  balance Decimal
  openedAt DateTime
  active Boolean
}

model Tag {
  id Uuid @id
  label String
}
