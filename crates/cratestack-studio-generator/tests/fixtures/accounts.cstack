enum EnrollmentRecordStatus {
  pending
  verified
}

model User {
  id String @id
  mainEmail String?
  enrollmentStatus EnrollmentRecordStatus

  @@allow("read", auth() != null)
}

type PublicUserLookupArgs {
  userId String
}

procedure publicUser(args: PublicUserLookupArgs): User?
