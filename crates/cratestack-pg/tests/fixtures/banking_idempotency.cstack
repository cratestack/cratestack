datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

auth Operator {
  id Int
}

model Voucher {
  id Int @id
  code String
  amount Int

  @@allow("read", auth() != null)
  @@allow("create", auth() != null)
}
