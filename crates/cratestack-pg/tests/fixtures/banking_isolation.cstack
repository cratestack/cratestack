datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

auth Operator {
  id Int
}

model Balance {
  id Int @id
  amount Int

  @@allow("read", auth() != null)
  @@allow("update", auth() != null)
}
