//! `tests/mcp_resources_pg.rs`'s modules (cratestack#1040), in a support
//! directory as `cratestack-api`'s `tests/mcp_support/` is, so each file
//! stays small and the driver reads as the list of what is proven.

pub mod adversarial;
pub mod harness;
pub mod paging;
pub mod parity;
