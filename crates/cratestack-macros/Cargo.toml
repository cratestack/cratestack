[package]
name = "cratestack-macros"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme = "README.md"
keywords.workspace = true
categories.workspace = true
# Silently ignored for *this* crate as a real workspace member — the root
# `[workspace] resolver = "3"` governs feature resolution here regardless.
# Exists purely for `tests/ui.rs`'s `trybuild`-based suite: `trybuild`
# copies this exact field into the standalone synthetic mini-workspace it
# generates per fixture run (`trybuild-1.0.120/src/run.rs`'s
# `build_manifest`), and *that* synthetic project — not being a member of
# our real workspace — actually obeys it. Resolver "1" (the pre-2021
# default, still fully supported) unifies host/target features into one
# pool; "3" (this crate's real, inherited default) splits them. That split
# is exactly what broke the suite: `trybuild` also copies this crate's own
# `[dependencies]` (`cratestack-core` included) onto the synthetic
# project's own target-context dependency list — separate from the
# proc-macro's host-context copy of the same package — and only the
# host-context one ever received this crate's `decimal-rust-decimal`
# forward, leaving the target-context copy selecting neither backend and
# hitting `cratestack-core`'s "enable exactly one" `compile_error!`.
# Forcing resolver "1" for the synthetic project only (see above) makes
# both copies unify into a single feature set again, the same way they
# did before cratestack#495 introduced a second, genuinely selectable
# backend. See `tests/ui.rs`'s own module doc for the parts of this that
# live on that side.
resolver = "1"

[lib]
proc-macro = true

[features]
default = ["decimal-rust-decimal"]
# Forwards to `cratestack-core`'s mutually-exclusive decimal backend choice
# (see that crate's Cargo.toml for the full rationale). Required because
# `cratestack-core`'s dependency edge has `default-features = false` at the
# workspace root — every direct consumer must explicitly re-select a
# backend, or `cratestack-core` fails to compile at all (cratestack#495).
# Note this crate is a proc-macro — this feature controls what THIS crate
# (the macro implementation) compiles against, not what the schema it
# expands generates; `cratestack::Decimal` in generated code always
# resolves through whichever facade the consumer picked, independent of
# this flag. Also forwards to `cratestack-policy`, `cratestack-parser`,
# and `cratestack-proto` — this crate's other direct `cratestack-core`-
# consuming dependencies (each with the same `default-features = false`
# treatment — see root `Cargo.toml`).
decimal-rust-decimal = [
    "cratestack-core/decimal-rust-decimal",
    "cratestack-policy/decimal-rust-decimal",
    "cratestack-parser/decimal-rust-decimal",
    "cratestack-proto/decimal-rust-decimal",
]
decimal-bigdecimal = [
    "cratestack-core/decimal-bigdecimal",
    "cratestack-policy/decimal-bigdecimal",
    "cratestack-parser/decimal-bigdecimal",
    "cratestack-proto/decimal-bigdecimal",
]
# Gates `transport grpc` Rust codegen under `include_server_schema!` — see
# `crates/cratestack-macros/src/include/reject_grpc.rs`. Checked at macro-
# expansion time via `cfg!(feature = "grpc")` against this crate's *own*
# compiled feature set (proc-macros can't read a consumer's
# `CARGO_FEATURE_*` env vars — those describe the crate being expanded
# *into*, not this one). Forwarded from `cratestack-pg`'s own `grpc`
# feature; see `docs/design/extensions.md` §2 for the mechanism this
# mirrors. Off by default — a plain `cargo check --workspace` never touches
# this path.
grpc = []
# Marks whether the facade crate being compiled into has `cratestack-sqlx`
# available at all — see
# `crates/cratestack-macros/src/include/datasource_guard.rs`'s
# `guard_server_postgres_backend` (cratestack#347). Same `cfg!(feature =
# "postgres")`-against-this-crate's-own-feature-set mechanism as `grpc`
# above. Forwarded from `cratestack-pg`'s own (default-on) `postgres`
# feature; `cratestack-api` never forwards it, so `db = Postgres` under that
# facade fails this guard with a clear `compile_error!` instead of a wall of
# unrelated "cannot find `sqlx`/`SqlxRuntime` in `cratestack`" errors.
postgres = []
# Gates codegen for the `rate_limit` extension, declared in `.cstack` via
# `extension rate_limit { }` (cratestack#153/#161). Same `cfg!(feature =
# "rate_limit")`-against-this-crate's-own-feature-set mechanism as `grpc`/
# `postgres` above — see `crates/cratestack-macros/src/include/extension_gate.rs`
# and `docs/design/extensions.md` §2. Forwarded from the `cratestack-pg`/
# `cratestack-sqlite` facades' own `rate_limit` feature (cratestack#154):
# enables the `@no_rate_limit` procedure attribute and threads
# `rate_limited_by_default` onto the generated `OpDescriptor` — participation
# only, never a numeric limit. Deliberately does *not* gate
# `cratestack-axum`'s existing `RateLimitLayer`/`RateLimitConfig`/store code,
# which stays unconditionally compiled (cratestack#154 scoped that move out;
# see its ticket and PR for the reasoning). Off by default.
rate_limit = []
# Gates codegen for the `pgvector` extension, declared in `.cstack` via
# `extension pgvector { }` (cratestack#153/#161). Same mechanism as
# `rate_limit` above — see `extension_gate.rs`. Intended to be forwarded
# from `cratestack-migrate`/`cratestack-sqlx`/`cratestack-pg`'s own
# `pgvector` feature once cratestack#155 wires that up. Note:
# `include_embedded_schema!` rejects `extension pgvector { }`
# unconditionally regardless of this feature — rusqlite has no vector
# extension, so no Cargo feature can make it valid there (see
# `extension_gate.rs`, `docs/design/extensions.md` §6). Off by default.
pgvector = []

[dependencies]
cratestack-core.workspace = true
cratestack-parser.workspace = true
cratestack-policy.workspace = true
# Not gated behind the `grpc` feature: unlike `tonic`/`prost` (pulled in by
# `cratestack-grpc`, only when a consumer opts in), `cratestack-proto` is
# lightweight (toml/serde/thiserror on top of `cratestack-core` — no
# protobuf runtime deps) and this crate needs its lock-reading/comparison
# logic (`PbLock`, `lock_would_change`) unconditionally to decide whether
# the `grpc` feature guard itself should even run.
cratestack-proto.workspace = true
proc-macro2.workspace = true
quote.workspace = true
sha2.workspace = true
syn.workspace = true

[dev-dependencies]
# UI/compile-fail testing for `extension_gate.rs`'s `compile_error!`
# diagnostics (cratestack#161) — trybuild spawns an isolated `rustc`
# invocation per fixture and asserts it fails to compile, which a plain
# `tests/*.rs` integration test can't do without hard-failing the whole
# `cargo test` run. Not used anywhere else in the workspace yet; scoped to
# this crate rather than added to `[workspace.dependencies]` since it's the
# first consumer.
trybuild = "1"
