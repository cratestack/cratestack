datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

model User {
  id Int @id
}

model Post {
  id Int @id
  authorId Int
  author User @relation(fields:[unknownField], references:[id])
}
