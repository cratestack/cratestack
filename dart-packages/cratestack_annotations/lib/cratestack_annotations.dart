/// Annotations consumed by CrateStack's Dart code generators.
///
/// Runtime-only and dependency-free — see `pubspec.yaml` for why this is a
/// separate package from `cratestack_builder`, which does the generating.
library;

export 'src/cratestack_builder_annotation.dart' show CratestackBuilder;
