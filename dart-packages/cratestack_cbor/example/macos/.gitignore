# Flutter-related
**/Flutter/ephemeral/
**/Pods/

# Xcode-related
**/dgph
**/xcuserdata/

# Generated by CocoaPods at build time from the plugin podspecs — not
# checked in. A real `flutter build macos` writes both; committing them
# would pin a resolved pod graph the example does not own.
/Podfile
/Podfile.lock
