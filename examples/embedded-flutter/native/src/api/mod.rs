//! `flutter_rust_bridge` discovers Dart-callable functions by scanning
//! the `api/` module tree. Anything that needs a Dart counterpart goes
//! under here; everything outside is private Rust.

pub mod notes;
