{
  "name": "cratestack-notes",
  "version": "0.1.0",
  "private": true,
  "description": "Expo native module wrapping the embedded-expo-native Rust cdylib. Routes a cratestack OperationRequest JSON envelope to native code via JSI/NativeModules.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "expo": "^55.0.0",
    "expo-modules-core": "^55.0.0",
    "react": ">=19.0.0",
    "react-native": ">=0.85.0"
  }
}
