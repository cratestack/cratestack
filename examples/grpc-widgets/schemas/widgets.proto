// Code generated by `cratestack generate-proto`. DO NOT EDIT BY HAND.
//
// Generated from `examples/grpc-widgets/schemas/widgets.cstack`, which declares `transport grpc`.
// This file describes both the message shapes AND, via the `service` block
// below, the RPC contract this schema's `transport grpc` binding serves — it
// IS the wire, not just a shape description. See the CrateStack gRPC runtime
// docs for current server/client support status.
//
// Field presence: every non-repeated field below is `optional` (proto3 explicit
// presence), representing CrateStack's partial `fields`/`include` projection
// where a field can be genuinely absent. `repeated` (list) fields are the
// documented exception — proto3 forbids `repeated optional` — so an empty
// list and an unprojected/absent list read identically on this wire. See
// docs/design/protobuf.md §4.4.

syntax = "proto3";

package widgets_api;

message CreateWidgetInput {
  optional int64 id = 1;
  optional string name = 2;
}

message EchoWidgetNameInput {
  optional string name = 1;
}

message EchoWidgetNameOutput {
  optional string result = 1;
}

message PageInfo {
  optional int64 limit = 1;
  optional int64 offset = 2;
  bool has_next_page = 3;
  bool has_previous_page = 4;
}

message PageOfWidget {
  repeated Widget items = 1;
  optional int64 total_count = 2;
  optional PageInfo page_info = 3;
}

message RpcListPredicate {
  optional string key = 1;
  optional string value = 2;
}

message StringList {
  repeated string values = 1;
}

message UpdateWidgetInput {
  optional string name = 1;
}

message Widget {
  optional int64 id = 1;
  optional string name = 2;
}

message WidgetNameSamplesInput {
}

message WidgetNameSamplesOutput {
  repeated string result = 1;
}

message WidgetRpcListInput {
  optional int64 limit = 1;
  optional int64 offset = 2;
  repeated string fields = 3;
  repeated string include = 4;
  map<string, StringList> include_fields = 5;
  optional string sort = 6;
  optional string where_expr = 7;
  optional string or = 8;
  repeated RpcListPredicate filters = 9;
}

message WidgetRpcPkInput {
  optional int64 id = 1;
}

message WidgetRpcUpdateInput {
  optional int64 id = 1;
  optional UpdateWidgetInput patch = 2;
}

service Api {
  rpc ModelWidgetList(WidgetRpcListInput) returns (PageOfWidget);
  rpc ModelWidgetGet(WidgetRpcPkInput) returns (Widget);
  rpc ModelWidgetCreate(CreateWidgetInput) returns (Widget);
  rpc ModelWidgetUpdate(WidgetRpcUpdateInput) returns (Widget);
  rpc ModelWidgetDelete(WidgetRpcPkInput) returns (Widget);
  rpc ProcedureEchoWidgetName(EchoWidgetNameInput) returns (EchoWidgetNameOutput);
  rpc ProcedureWidgetNameSamples(WidgetNameSamplesInput) returns (stream WidgetNameSamplesOutput);
}
